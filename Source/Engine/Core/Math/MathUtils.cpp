#include "MathUtils.h"

namespace nc
{
    glm::vec3 nc::QuaternionToEuler(const glm::quat& q)
    {
        return glm::vec3();
    }

    glm::quat nc::EulerToQuaternion(const glm::vec3& euler)
    {
        return glm::quat();
    }
}